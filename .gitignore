# Secrets & local-only working files (do NOT commit)
.streamlit/secrets.toml
.env
.env.*
!.env.example
*.pem
*.key
# 서비스 계정 JSON — Streamlit secrets 로 넣는 값이라 파일이 굴러다닐 이유가 없다
service-account*.json
gcp-*.json

# OS / cache / build output
.DS_Store
__pycache__/
*.pyc
graphify-out/

.cache_results.json
evals/.cache_results.json
evals/.watch_log.txt

# 일일 토큰 사용 원장 (로컬 실행분 집계 — 머신마다 다르다)
evals/.daily_usage.json
